# ============================================================================
#  MailArchiver — обязательные зависимости
#  Устанавливаются автоматически скриптом install.sh в изолированное
#  виртуальное окружение (venv). Версии зафиксированы для воспроизводимости.
# ============================================================================
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
apscheduler==3.11.0
IMAPClient==3.0.1
cryptography==44.0.0
PyYAML==6.0.2

# ----------------------------------------------------------------------------
#  Необязательные зависимости (устанавливаются по желанию):
#
#  Aspose.Email — надёжный экспорт в .pst (ANSI/Unicode). Коммерческая
#  лицензия для промышленного использования. Установка:
#      pip install Aspose.Email-for-Python-via-NET
#  Требует установленного .NET Runtime на сервере (см. docs/ru/08-pst-export.md).
#
#  httpx, pytest — только для запуска тестов (tests/), в проде не нужны.
# ----------------------------------------------------------------------------
