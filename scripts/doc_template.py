# -*- coding: utf-8 -*-
"""Шаблон HTML-документации (используется scripts/gen_docs.py)."""

TEMPLATE = r"""<!doctype html>
<html lang="ru">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>MailArchiver — документация</title>
<style>
:root{--pri:#2f6fed;--bg:#fff;--soft:#f4f6fb;--bd:#e2e8f2;--tx:#1c2430;--dim:#5a6675;--ok:#1f9d55;--warn:#d98a00;--danger:#d64545;--radius:10px;}
*{box-sizing:border-box}
body{margin:0;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Arial,sans-serif;color:var(--tx);background:var(--bg);line-height:1.62;font-size:16px}
a{color:var(--pri);text-decoration:none}a:hover{text-decoration:underline}
code{font-family:ui-monospace,"Cascadia Code",Consolas,monospace;background:var(--soft);padding:1.5px 6px;border-radius:5px;font-size:.88em}
pre.cmd{background:#0d1017;color:#d7e0ee;padding:15px 18px;border-radius:var(--radius);overflow-x:auto;font-family:ui-monospace,Consolas,monospace;font-size:.86rem;line-height:1.55}
pre.cmd .c{color:#6b7a90}
.layout{display:flex;max-width:1200px;margin:0 auto;gap:32px;padding:0 20px}
.toc{position:sticky;top:0;align-self:flex-start;height:100vh;overflow-y:auto;width:260px;flex:0 0 260px;padding:24px 0;border-right:1px solid var(--bd)}
.toc .logo{display:flex;align-items:center;gap:10px;font-weight:800;font-size:1.15rem;margin-bottom:2px;padding:0 8px}
.toc .logo .b{width:30px;height:30px;border-radius:8px;background:linear-gradient(135deg,#2f6fed,#7db0ff);color:#fff;display:flex;align-items:center;justify-content:center}
.toc .ver{color:var(--dim);font-size:.78rem;padding:0 8px 14px;margin-left:40px;margin-top:-6px}
.toc a{display:block;padding:6px 10px;color:var(--dim);border-radius:7px;font-size:.9rem}
.toc a:hover{background:var(--soft);color:var(--tx);text-decoration:none}
.toc a.h2{padding-left:22px;font-size:.85rem}
.content{flex:1;min-width:0;padding:30px 0 90px;max-width:820px}
h1{font-size:2rem;letter-spacing:-.02em;margin:.2em 0 .4em}
h2{font-size:1.5rem;margin:1.9em 0 .5em;padding-top:12px;border-top:2px solid var(--soft);letter-spacing:-.01em}
h3{font-size:1.18rem;margin:1.4em 0 .4em}
h3.pg{margin-top:1.6em;color:var(--pri)}
p{margin:.6em 0}
ul,ol{margin:.5em 0 .9em;padding-left:1.4em}li{margin:.25em 0}
img.shot{width:100%;border:1px solid var(--bd);border-radius:var(--radius);box-shadow:0 4px 18px rgba(20,30,50,.10);margin:12px 0 6px}
.cap{color:var(--dim);font-size:.85rem;text-align:center;margin-bottom:14px}
.lead{font-size:1.1rem;color:var(--dim)}
.note,.warn,.tip{border-radius:var(--radius);padding:13px 16px;margin:14px 0;border-left:4px solid}
.note{background:#eef4ff;border-color:var(--pri)}
.warn{background:#fdf3df;border-color:var(--warn)}
.tip{background:#e3f6ec;border-color:var(--ok)}
.note b,.warn b,.tip b{display:block;margin-bottom:2px}
table.t{width:100%;border-collapse:collapse;margin:12px 0;font-size:.92rem}
table.t th{background:var(--soft);text-align:left;padding:9px 12px;border:1px solid var(--bd)}
table.t td{padding:9px 12px;border:1px solid var(--bd);vertical-align:top}
.steps{counter-reset:s;list-style:none;padding:0}
.steps>li{counter-increment:s;position:relative;padding:2px 0 14px 44px;margin:0}
.steps>li::before{content:counter(s);position:absolute;left:0;top:0;width:30px;height:30px;background:var(--pri);color:#fff;border-radius:50%;display:flex;align-items:center;justify-content:center;font-weight:700;font-size:.9rem}
.grid2{display:grid;grid-template-columns:1fr 1fr;gap:16px}
.card{border:1px solid var(--bd);border-radius:var(--radius);padding:16px;background:#fff}
.diagram{background:var(--soft);border:1px solid var(--bd);border-radius:var(--radius);padding:18px;margin:16px 0;overflow-x:auto}
.badge{display:inline-block;padding:1px 8px;border-radius:12px;font-size:.78rem;font-weight:600}
.badge.ok{background:#e3f6ec;color:var(--ok)}.badge.exp{background:#fdf3df;color:var(--warn)}
.ptable{border:1px solid var(--bd);border-radius:var(--radius);overflow:hidden;margin:10px 0 20px}
.prow{display:grid;grid-template-columns:230px 1fr;border-top:1px solid var(--bd)}
.prow:first-child{border-top:none}
.pname{padding:12px 14px;background:var(--soft);border-right:1px solid var(--bd)}
.ptitle{display:block;font-weight:650}.pname code{background:#fff;font-size:.76rem;color:var(--dim);display:inline-block;margin-top:4px}
.pdesc{padding:12px 14px}.pdesc p{margin:.2em 0}
.prec{color:var(--ok);font-size:.92rem}.pmeta{color:var(--dim);font-size:.86rem}
@media(max-width:860px){.toc{display:none}.grid2{grid-template-columns:1fr}.prow{grid-template-columns:1fr}.pname{border-right:none;border-bottom:1px solid var(--bd)}}
</style>
</head>
<body>
<div class="layout">
<nav class="toc">
  <div class="logo"><span class="b">📥</span> MailArchiver</div>
  <div class="ver">Документация</div>
  <a href="#about">1. О программе</a>
  <a href="#how">2. Как это работает</a>
  <a href="#install">3. Установка</a>
  <a href="#install" class="h2">— через скрипт (systemd)</a>
  <a href="#docker" class="h2">— через Docker</a>
  <a href="#firstrun">4. Первый вход</a>
  <a href="#firstrun" class="h2">— вход по ящику</a>
  <a href="#dashboard">5. Дашборд и мониторинг</a>
  <a href="#mailview">6. Просмотр писем</a>
  <a href="#search" class="h2">— поиск по письмам</a>
  <a href="#analytics">7. Аналитика</a>
  <a href="#analytics" class="h2">— состояние системы</a>
  <a href="#mailanalytics" class="h2">— аналитика писем</a>
  <a href="#dedup" class="h2">— одинаковые вложения</a>
  <a href="#accounts">8. Добавление ящика</a>
  <a href="#providers" class="h2">— настройки провайдеров</a>
  <a href="#mailboxes" class="h2">— пароли и даты копий</a>
  <a href="#mailadmin" class="h2">— вход через администратора</a>
  <a href="#employees">9. Сотрудники</a>
  <a href="#dismissed" class="h2">— уволенные сотрудники</a>
  <a href="#backup">10. Резервное копирование</a>
  <a href="#export">11. Экспорт в PST</a>
  <a href="#restore">12. Восстановление</a>
  <a href="#retention">— хранение копий (3 дня/неделя)</a>
  <a href="#schedules">13. Расписания</a>
  <a href="#settings">14. Настройки (все параметры)</a>
  <a href="#jobs">15. Очередь и логи</a>
  <a href="#security">16. Безопасность</a>
  <a href="#twofa" class="h2">— двухфакторный вход</a>
  <a href="#encryption" class="h2">— шифрование копии</a>
  <a href="#secjournal" class="h2">— журнал безопасности</a>
  <a href="#replica">17. Копия вне сервера</a>
  <a href="#restorecopy" class="h2">— восстановление после аварии</a>
  <a href="#monitoring">18. Мониторинг</a>
  <a href="#trouble">19. Устранение неполадок</a>
  <a href="#faq">20. Частые вопросы</a>
  <a href="#api">21. API</a>
</nav>
<main class="content">

<h1 id="about">MailArchiver — документация</h1>
<p class="lead">Сервис резервного копирования почтовых ящиков по IMAP с веб-интерфейсом, очередью
заданий, планировщиком, подробной статистикой и экспортом копий в формат <b>.pst</b> для Microsoft Outlook.</p>

<p>Эта документация написана так, чтобы ей мог воспользоваться <b>даже не специалист</b>. Если вы
умеете копировать команды в терминал и заполнять формы в браузере — вы справитесь. Сложные места
снабжены пояснениями и подсказками.</p>

<div class="tip"><b>💡 Что делает программа простыми словами</b>
Она регулярно и автоматически «скачивает» все письма из ваших почтовых ящиков и хранит их локальную
копию на вашем сервере. Из этой копии можно в любой момент получить файл <code>.pst</code> и открыть его
в Outlook, либо «залить» письма обратно на почтовый сервер, если они пропали.</p></div>

<h3>Ключевые возможности</h3>
<ul>
<li><b>Резервное копирование по IMAP</b> — инкрементальное (докачиваются только новые письма), с сохранением папок и флагов.</li>
<li><b>Расписание</b> — копирование запускается автоматически (например, каждую ночь).</li>
<li><b>Экспорт в .pst</b> для всех версий Outlook (ANSI для 97–2002, Unicode для 2003+), а также в <b>EML</b> и <b>MBOX</b>.</li>
<li><b>Восстановление</b> писем из копии обратно на любой IMAP-сервер.</li>
<li><b>Импорт из .pst</b> — залить существующий PST-файл на почтовый сервер.</li>
<li><b>Просмотр писем</b> прямо в интерфейсе (папки, список, чтение с вложениями и HTML).</li>
<li><b>Вход по ящику</b> — по email и паролю самого ящика; такой пользователь видит только свой ящик.</li>
<li><b>Хранение копий за 3 дня / 1 неделю</b> от текущей даты (пресеты очистки на каждый ящик).</li>
<li><b>Веб-интерфейс</b> с живой статистикой, очередью, логами и подсказкой к каждому параметру.</li>
<li><b>Очередь заданий</b> с приоритетами, ограничением нагрузки и повторными попытками при сбоях.</li>
<li><b>Безопасность</b> — вход по паролю, двухфакторный вход, защита от подбора, журнал безопасности,
шифрование паролей ящиков и — по желанию — самих писем на диске.</li>
<li><b>Копия вне сервера</b> — каждый день в сетевую папку, на другой сервер по SSH или в S3, со снимками базы.</li>
<li><b>Мониторинг</b> — метрики для Prometheus и Zabbix, еженедельная сводка на почту.</li>
<li><b>Поиск по письмам</b> во всех ящиках — по теме, адресам, вложениям и тексту.</li>
<li><b>Вход в ящики через администратора почты</b> — без сбора паролей сотрудников (Dovecot, Cyrus, Zimbra).</li>
<li><b>Уволенные сотрудники</b> — последняя копия, выключение копирования и удержание архива на годы.</li>
<li><b>Отчёт «Одинаковые вложения»</b> — сколько места сэкономит хранение одной копии рассылок и логотипов.</li>
</ul>

<h2 id="how">2. Как это работает (методология)</h2>
<p>MailArchiver состоит из нескольких взаимодействующих частей. Понимание общей схемы поможет
настроить сервис под ваши задачи.</p>

<div class="diagram">
<svg viewBox="0 0 760 300" width="760" xmlns="http://www.w3.org/2000/svg" font-family="sans-serif" font-size="13">
  <rect x="20" y="120" width="150" height="60" rx="8" fill="#e6efff" stroke="#2f6fed"/>
  <text x="95" y="146" text-anchor="middle" font-weight="700">Веб-интерфейс</text>
  <text x="95" y="164" text-anchor="middle" fill="#5a6675">браузер</text>

  <rect x="220" y="30" width="150" height="55" rx="8" fill="#fff" stroke="#2f6fed"/>
  <text x="295" y="54" text-anchor="middle" font-weight="700">Планировщик</text>
  <text x="295" y="71" text-anchor="middle" fill="#5a6675">по расписанию</text>

  <rect x="220" y="120" width="150" height="60" rx="8" fill="#fff" stroke="#2f6fed"/>
  <text x="295" y="146" text-anchor="middle" font-weight="700">Очередь</text>
  <text x="295" y="164" text-anchor="middle" fill="#5a6675">воркеры</text>

  <rect x="220" y="215" width="150" height="55" rx="8" fill="#fff" stroke="#2f6fed"/>
  <text x="295" y="239" text-anchor="middle" font-weight="700">База (SQLite)</text>
  <text x="295" y="256" text-anchor="middle" fill="#5a6675">индекс, задания</text>

  <rect x="430" y="120" width="150" height="60" rx="8" fill="#fff" stroke="#1f9d55"/>
  <text x="505" y="146" text-anchor="middle" font-weight="700">IMAP-движок</text>
  <text x="505" y="164" text-anchor="middle" fill="#5a6675">бэкап/восстан.</text>

  <rect x="620" y="60" width="120" height="55" rx="8" fill="#e3f6ec" stroke="#1f9d55"/>
  <text x="680" y="84" text-anchor="middle" font-weight="700">Почтовый</text>
  <text x="680" y="101" text-anchor="middle">сервер</text>

  <rect x="620" y="150" width="120" height="55" rx="8" fill="#f4f6fb" stroke="#5a6675"/>
  <text x="680" y="174" text-anchor="middle" font-weight="700">Хранилище</text>
  <text x="680" y="191" text-anchor="middle" fill="#5a6675">Maildir/.eml</text>

  <line x1="170" y1="150" x2="220" y2="150" stroke="#5a6675" marker-end="url(#a)"/>
  <line x1="295" y1="85" x2="295" y2="120" stroke="#5a6675" marker-end="url(#a)"/>
  <line x1="295" y1="180" x2="295" y2="215" stroke="#5a6675" marker-end="url(#a)"/>
  <line x1="370" y1="150" x2="430" y2="150" stroke="#5a6675" marker-end="url(#a)"/>
  <line x1="580" y1="140" x2="620" y2="100" stroke="#1f9d55" marker-end="url(#a)"/>
  <line x1="580" y1="160" x2="620" y2="178" stroke="#5a6675" marker-end="url(#a)"/>
  <defs><marker id="a" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto"><path d="M0,0 L8,4 L0,8" fill="#5a6675"/></marker></defs>
</svg>
</div>

<h3>Резервное копирование: инкрементальный алгоритм</h3>
<p>При копировании MailArchiver не скачивает всё заново каждый раз. Он использует стандартные
механизмы IMAP:</p>
<ul>
<li>у каждого письма в папке есть уникальный номер <b>UID</b>;</li>
<li>у папки есть признак <b>UIDVALIDITY</b> — если он изменился, папка перечитывается целиком (так требует протокол);</li>
<li>сервис помнит, какие UID уже сохранены, и <b>докачивает только новые</b> письма.</li>
</ul>
<p>Каждое письмо сохраняется отдельным файлом в формате <b>Maildir</b> (стандарт, который понимают многие
почтовые программы). Отдельный файл на письмо — это надёжно: повреждение одного файла не затрагивает
остальные, копию легко просматривать и переносить. После записи размер и контрольная сумма (SHA-256)
сверяются — это защищает от «тихой» порчи данных.</p>

<div class="diagram">
<svg viewBox="0 0 720 120" width="720" xmlns="http://www.w3.org/2000/svg" font-family="sans-serif" font-size="13">
  <rect x="10" y="35" width="140" height="50" rx="8" fill="#e3f6ec" stroke="#1f9d55"/><text x="80" y="65" text-anchor="middle" font-weight="700">Почтовый сервер</text>
  <rect x="230" y="35" width="150" height="50" rx="8" fill="#fff" stroke="#2f6fed"/><text x="305" y="60" text-anchor="middle" font-weight="700">Воркер</text><text x="305" y="76" text-anchor="middle" fill="#5a6675" font-size="11">UID FETCH пакетами</text>
  <rect x="460" y="10" width="150" height="45" rx="8" fill="#f4f6fb" stroke="#5a6675"/><text x="535" y="37" text-anchor="middle" font-weight="700">Maildir (.eml)</text>
  <rect x="460" y="70" width="150" height="45" rx="8" fill="#f4f6fb" stroke="#5a6675"/><text x="535" y="97" text-anchor="middle" font-weight="700">Индекс (SQLite)</text>
  <line x1="150" y1="60" x2="230" y2="60" stroke="#5a6675" marker-end="url(#b)"/><text x="190" y="52" text-anchor="middle" fill="#5a6675" font-size="11">только новые</text>
  <line x1="380" y1="55" x2="460" y2="35" stroke="#5a6675" marker-end="url(#b)"/>
  <line x1="380" y1="65" x2="460" y2="90" stroke="#5a6675" marker-end="url(#b)"/>
  <defs><marker id="b" markerWidth="9" markerHeight="9" refX="7" refY="4" orient="auto"><path d="M0,0 L8,4 L0,8" fill="#5a6675"/></marker></defs>
</svg>
</div>

<h3>Очередь и планировщик</h3>
<p>Любая тяжёлая операция (копирование, экспорт, восстановление) ставится в <b>очередь</b> и выполняется
фоновыми воркерами. Это позволяет: видеть прогресс в реальном времени, не перегружать почтовый сервер
(лимит одновременных подключений на ящик), безопасно повторять при сбоях. Планировщик по расписанию сам
ставит задания в очередь — например, «каждый день в 3:00 сделать бэкап всех ящиков».</p>

<h2 id="install">3. Установка</h2>
<p>Есть два способа. Выберите один.</p>

<h3>Способ А. Установка скриптом (служба systemd) — рекомендуется для сервера</h3>
<p>Подходит для обычного Linux-сервера (Debian/Ubuntu, RHEL/Rocky/Alma/Fedora, openSUSE, Arch).
Скрипт сам определит дистрибутив, установит зависимости, создаст службу и настроит автозапуск.</p>
<ol class="steps">
<li>Скопируйте папку проекта на сервер (например, через <code>scp</code> или <code>git</code>) и зайдите в неё:
<pre class="cmd">cd mail_backup</pre></li>
<li>Запустите установку с правами администратора:
<pre class="cmd">sudo bash scripts/install.sh</pre>
Скрипт установит Python-зависимости в изолированное окружение, создаст пользователя <code>mailarchiver</code>,
службу и файл настроек <code>/etc/mailarchiver/config.yaml</code>.</li>
<li>В конце установки появится запрос на создание администратора — введите логин и пароль. Если установка
шла без интерактивного ввода, создайте его вручную:
<pre class="cmd">sudo -u mailarchiver /opt/mailarchiver/venv/bin/mailarchiver \
  -c /etc/mailarchiver/config.yaml create-admin</pre></li>
<li>Готово. Веб-интерфейс доступен на <code>http://127.0.0.1:8493</code>.</li>
</ol>
<div class="note"><b>Полезные команды службы</b>
<pre class="cmd">systemctl status mailarchiver     <span class="c"># состояние</span>
journalctl -u mailarchiver -f      <span class="c"># смотреть логи в реальном времени</span>
systemctl restart mailarchiver     <span class="c"># перезапуск</span></pre></div>
<div class="warn"><b>⚠ Доступ снаружи</b>
По умолчанию сервис слушает только <code>127.0.0.1</code> (локально) — это безопасно. Чтобы открыть доступ
из сети, настройте reverse proxy (nginx/traefik) с HTTPS (см. раздел «Безопасность»), а не открывайте порт напрямую.</p></div>

<h3 id="docker">Способ Б. Docker Compose — самый простой</h3>
<p>Если на сервере есть Docker, это самый быстрый путь: все зависимости уже внутри образа.</p>
<ol class="steps">
<li>Зайдите в папку <code>docker</code> проекта и при необходимости откройте <code>docker-compose.yml</code>,
чтобы задать логин/пароль администратора (переменные <code>MA_ADMIN_USER</code> и <code>MA_ADMIN_PASSWORD</code>).</li>
<li>Соберите и запустите контейнер:
<pre class="cmd">cd docker
docker compose up -d</pre></li>
<li>Откройте <code>http://ВАШ_СЕРВЕР:8493</code> и войдите под заданными логином/паролем.</li>
</ol>
<div class="tip"><b>💡 Где хранятся данные</b>
Все копии писем и настройки лежат в томе <code>mailarchiver-data</code> и переживают перезапуск и обновление
контейнера. Экспортированные файлы можно забирать через веб-интерфейс (кнопка «Скачать») или смонтировать
хост-каталог в <code>/data/exports</code>.</p></div>

<h2 id="firstrun">4. Первый вход</h2>
<p>При первом открытии веб-интерфейса вам предложат создать администратора (если он ещё не создан). Затем —
обычная форма входа.</p>
<div class="warn"><b>⚠ Первичную настройку откройте по IP-адресу</b>
Мастер создания первого администратора работает, только если интерфейс открыт по IP-адресу,
<code>localhost</code>, короткому имени без точки или по имени из «Внешний URL» (<code>server.public_url</code>).
Это защита от атаки DNS rebinding: чужой сайт не сможет создать администратора раньше вас. Обычно
достаточно SSH-туннеля и адреса <code>http://127.0.0.1:8493</code>.</div>
<img class="shot" src="img/01_login.png" alt="Экран входа">
<div class="cap">Экран входа в веб-интерфейс</div>
<div class="tip"><b>🔑 Два способа входа</b>
<p><b>1. Администратор</b> — логин и пароль, заданные при первичной настройке; видит и настраивает всё.</p>
<p><b>2. Пользователь-ящик</b> — вход по <b>email и паролю самого почтового ящика</b> (пароль проверяется прямым
подключением к IMAP). Такой пользователь видит только свой ящик: может просматривать письма, запускать
копирование, экспорт и восстановление своего ящика (раздел «Почта» → «Выгрузить» / «Восстановить в ящик»),
но не может менять срок хранения и расписания, загружать .pst и не видит чужие ящики, общий журнал и логины
администраторов. Ящик должен быть заранее добавлен администратором и включён.</p>
<p>Число проверок пароля ящика на почтовом сервере ограничено (10 на ящик и 60 на все ящики за 15 минут),
чтобы страницу входа нельзя было использовать для подбора паролей почты и чтобы защита почтового сервера
(fail2ban) не заблокировала адрес архива. Если этот вход не нужен — выключите «Настройки → Безопасность →
Вход сотрудников по паролю ящика».</p></div>

<h2 id="dashboard">5. Дашборд и мониторинг</h2>
<p>Главный экран показывает всё важное сразу: число ящиков, писем в архиве, что сейчас в очереди/работе,
свободное место, живую ленту текущих операций с прогрессом, график активности и список ящиков с кнопкой
быстрого копирования.</p>
<img class="shot" src="img/02_dashboard.png" alt="Дашборд">
<div class="cap">Дашборд: статистика и текущие операции обновляются в реальном времени</div>
<p>Блок <b>«Текущие операции»</b> обновляется автоматически (через WebSocket). Для каждой операции виден
процент выполнения, скорость, объём и кнопка отмены.</p>

<h2 id="mailview">6. Просмотр писем</h2>
<p>Раздел <b>«Почта»</b> — это встроенный почтовый клиент для чтения писем из локальной копии, без
подключения к серверу. Слева — папки, посередине — список писем, справа — само письмо.</p>
<img class="shot" src="img/13_mail.png" alt="Просмотр писем">
<div class="cap">Просмотр писем: папки → список → чтение письма</div>
<p>Возможности просмотра:</p>
<ul>
<li>чтение текста письма и HTML-версии (HTML открывается в изолированной «песочнице» — скрипты не выполняются, это безопасно);</li>
<li>заголовки: от кого, кому, копия, дата;</li>
<li>вложения — со скачиванием по клику; картинки, вложенные в само письмо (логотип в подписи,
скриншот в тексте), показываются прямо в тексте — они берутся из архива, а не из интернета;</li>
<li>скачивание письма целиком в формате <code>.eml</code>;</li>
<li>непрочитанные письма выделены жирным, помеченные — звёздочкой; постранично.</li>
</ul>
<div class="note"><b>Где взять письма для просмотра</b>
Список формируется из локальной копии, поэтому сначала выполните резервное копирование ящика. Открыть
просмотр можно из раздела «Почта» (администратор выбирает ящик сверху) или из меню ящика (⋯ →
«Просмотр писем»). Пользователь, вошедший по учётным данным ящика, сразу попадает в просмотр своей почты.</div>
<img class="shot" src="img/24_mail_cid.png" alt="Письмо с логотипом в подписи">
<div class="cap">Логотип из подписи показывается сразу: картинка берётся из самого письма, внешнее не загружается</div>

<h3 id="search">Поиск по письмам</h3>
<p>Над списком писем — строка поиска: по теме, отправителю и получателям, именам вложений и тексту писем.
Администратор может искать <b>во всех ящиках сразу</b>, сотрудник — только в своём.</p>
<img class="shot" src="img/20_search.png" alt="Поиск по всем ящикам">
<div class="cap">Поиск «акт сверки» во всех ящиках: найденные слова подсвечены, клик открывает письмо</div>
<table class="t">
<tr><th>Запрос</th><th>Что найдёт</th></tr>
<tr><td><code>счёт оплата</code></td><td>письма, где есть оба слова; слова ищутся по началу («счет» найдёт «счета»), ё = е</td></tr>
<tr><td><code>"акт сверки"</code></td><td>слова подряд</td></tr>
<tr><td><code>тема:договор</code>, <code>от:ivanov</code>, <code>кому:petrov</code></td><td>только в теме, в отправителе, в получателях</td></tr>
<tr><td><code>вложение:прайс</code>, <code>текст:реквизиты</code></td><td>в имени вложения, только в тексте письма</td></tr>
</table>
<p>Письма заранее разбираются в <b>индекс</b> (SQLite FTS5) — поэтому поиск мгновенный. Новые письма
индексируются в фоне каждые 10 минут; первое индексирование большого архива идёт часы, но искать можно сразу —
по уже проиндексированной части. При включённом шифровании копии <b>текст</b> писем по умолчанию не
индексируется (индекс в базе открыт): ищутся тема, адреса и имена вложений. <b>Каждый поиск администратора
записывается в аудит</b> — это доступ к чужой переписке.</p>

<h2 id="analytics">7. Аналитика</h2>
<p>Два раздела аналитики доступны <b>только администратору</b>. Пользователь, вошедший по адресу и
паролю почтового ящика, их не видит. Все графики рисуются прямо в браузере, без обращения к внешним
сервисам — интерфейс полностью работает в закрытом контуре.</p>

<h3>Раздел «Аналитика» — состояние системы</h3>
<p>Отвечает на вопрос «как работает сервис»: сколько хранится, что происходит с заданиями, не
заканчивается ли место. Сверху выбираются <b>область</b> (все ящики или один) и <b>период</b>
графика активности (30 дней, 90 дней, полгода, год).</p>
<img class="shot" src="img/27_analytics.png" alt="Аналитика системы">
<div class="cap">«Аналитика»: плитки состояния и активность по дням</div>
<p>Плитки показывают: письма в архиве, объём копий и средний размер письма, число ящиков и папок,
задания и долю успешных, прогоны копирования, экспорты, расписания, свободное место, размер базы,
пользователей и сессии, состояние планировщика.</p>
<ul>
<li><b>Активность по дням</b> — график с переключателем метрики: письма, объём, задания, ошибки;</li>
<li><b>Задания по статусам и типам</b> — кольцевая диаграмма и столбцы;</li>
<li><b>Длительность заданий</b> — среднее и максимальное время по типам (рост средней длительности
бэкапа — повод проверить сеть или почтовый сервер);</li>
<li><b>Экспорт по форматам и движкам</b>, <b>хранилище по ящикам</b>, <b>ближайшие запуски</b>
расписаний и <b>топ действий</b> из журнала аудита.</li>
</ul>
<div class="note"><b>На что смотреть в первую очередь</b>
Доля успешных заданий заметно ниже 100 %, ненулевые ошибки на графике активности и мало свободного
места — три признака того, что с резервным копированием нужно разбираться сейчас.</div>

<h3 id="mailanalytics">Раздел «Аналитика писем» — содержание почты</h3>
<p>Отвечает на вопрос «что лежит в ящике»: кто пишет, когда, насколько объёмные письма, о чём они.
Сверху выбирается ящик или «Все ящики». Раздел состоит из двух частей.</p>
<img class="shot" src="img/28_mailanalytics.png" alt="Аналитика писем">
<div class="cap">«Аналитика писем»: показатели по индексу писем и динамика</div>

<p><b>Быстрая часть</b> считается по индексу писем (тема, отправитель, дата, размер, флаги, папка) и
работает мгновенно даже на больших ящиках:</p>
<ul>
<li>всего писем и период, объём, средний и медианный размер, число отправителей и доменов;</li>
<li>доля писем с вложениями и доля непрочитанных, ответы и пересылки, важные письма;</li>
<li><b>динамика по месяцам</b>, распределение <b>по годам</b>, <b>дням недели</b> и <b>часам суток</b>;</li>
<li><b>тепловая карта «день недели × час»</b> — наглядно показывает рабочие часы и активность в выходные;</li>
<li><b>гистограмма размеров</b> — что именно занимает место;</li>
<li><b>топ отправителей и доменов</b>, разбивка <b>по папкам</b>, <b>облако частых слов</b> из тем;</li>
<li><b>крупнейшие письма</b> — таблица самых тяжёлых писем.</li>
</ul>

<p><b>Глубокий анализ содержимого</b> вскрывает каждое письмо (файл <code>.eml</code>) и извлекает то,
чего нет в индексе. Это тяжёлая операция, поэтому она вынесена в фоновое задание: нажмите
«Запустить анализ» (или «Пересчитать»), а прогресс смотрите в разделе «Очередь и задания».
Результат сохраняется и при следующем открытии показывается сразу, с датой расчёта.</p>
<ul>
<li>число вложений, их объём и в скольких письмах встречаются;</li>
<li><b>типы вложений</b> — по расширению (pdf, docx, xlsx, zip…) и по MIME-типу;</li>
<li><b>домены получателей</b> — кому писали (поля «Кому» и «Копия»);</li>
<li><b>формат тела</b> (текст / HTML / оба) и <b>язык</b> (русский, латиница, смешанный);</li>
<li>средняя длина текста и <b>облако частых слов</b> по содержимому писем.</li>
</ul>
<div class="note"><b>Сколько это занимает</b>
Анализ читает каждое письмо с диска: ориентировочно 3–4 секунды на 650 писем, на ящике в 100 тысяч
писем — несколько минут. Задание можно отменить в любой момент; на резервное копирование оно не влияет.
Аналитика только читает данные — ни на сервере, ни в локальных копиях ничего не изменяется.</div>

<h3 id="dedup">Одинаковые вложения — сколько места сэкономит хранение одной копии</h3>
<p>Прайс, разосланный всем сотрудникам, — это сотни одинаковых PDF; логотип в подписи — картинка в каждом
письме; договор, пересланный по цепочке, — ещё десяток копий. Блок <b>«🧬 Одинаковые вложения»</b> внизу раздела
«Аналитика» считает, сколько места освободилось бы, если хранить каждое одинаковое вложение <b>один раз</b>.
<b>Формат хранения при этом не меняется</b> — это только подсчёт, чтобы решить, стоит ли менять хранение.</p>
<img class="shot" src="img/22_dedup.png" alt="Отчёт об одинаковых вложениях">
<div class="cap">Главная цифра — «Освободится»; ниже — самые повторяющиеся файлы, разбивка по типам и ящикам</div>
<ul>
<li>Вложения сравниваются <b>по содержимому</b> (отпечаток SHA-256): одинаковыми считаются файлы с одним
содержимым, даже если имена разные. Вложения меньше 1 КБ не учитываются.</li>
<li>«Посчитать» ставит фоновое задание. Первый подсчёт читает весь архив (часы на большом архиве), идёт частями
и пропускает вперёд копирования ящиков; отчёт по прочитанной части виден сразу. Следующие подсчёты читают
только новые письма.</li>
<li>«Только внутри ящиков» — экономия, если объединять копии лишь в пределах одного ящика: обычно она намного
меньше, основной выигрыш — на рассылках всем сотрудникам. Отдельно показано, сколько дало бы хранение
вложений без кодировки base64 (она увеличивает файлы на треть), и письма-двойники целиком.</li>
<li>Кнопка ✉️ в таблице открывает одно из писем с этим вложением.</li>
</ul>

<h2 id="accounts">8. Добавление почтового ящика</h2>
<p>Раздел <b>«Почтовые ящики»</b> → кнопка <b>«Добавить ящик»</b>. Заполните форму. У каждого поля есть
подсказка «?» — наведите на неё, чтобы прочитать описание, рекомендацию и пример.</p>
<img class="shot" src="img/04_account_modal.png" alt="Форма добавления ящика">
<div class="cap">Форма ящика. Наведение на «?» показывает подсказку к полю</div>
<ol class="steps">
<li>Введите <b>название</b> (любое понятное имя), <b>адрес IMAP-сервера</b> и <b>порт</b>.</li>
<li>Выберите <b>шифрование</b> (обычно SSL/TLS, порт 993).</li>
<li>Укажите <b>логин</b> и <b>пароль</b>. Для большинства провайдеров нужен «пароль приложения» (см. ниже).</li>
<li>Нажмите <b>«Проверить подключение»</b> — сервис попробует войти и покажет список папок.</li>
<li>Сохраните. Теперь ящик можно копировать.</li>
</ol>
<div class="warn"><b>⚠ Пароли приложений</b>
Яндекс, Mail.ru, Gmail и другие часто не принимают обычный пароль для сторонних программ. Нужно включить
доступ по IMAP в настройках почты и создать отдельный «пароль приложения». Gmail и Microsoft 365, как
правило, требуют вход через <b>OAuth2</b> — выберите этот способ и заполните соответствующие поля.</p></div>

<h3 id="providers">Настройки популярных провайдеров</h3>
<table class="t">
<tr><th>Провайдер</th><th>IMAP-сервер</th><th>Порт / шифрование</th><th>Примечание</th></tr>
<tr><td>Яндекс.Почта</td><td>imap.yandex.ru</td><td>993 / SSL</td><td>Включить IMAP; пароль приложения</td></tr>
<tr><td>Mail.ru</td><td>imap.mail.ru</td><td>993 / SSL</td><td>Пароль для внешних приложений</td></tr>
<tr><td>Gmail</td><td>imap.gmail.com</td><td>993 / SSL</td><td>OAuth2 или пароль приложения (с 2FA)</td></tr>
<tr><td>Microsoft 365 / Outlook</td><td>outlook.office365.com</td><td>993 / SSL</td><td>Обычно только OAuth2</td></tr>
<tr><td>Свой сервер (Dovecot и т.п.)</td><td>адрес вашего сервера</td><td>993 / SSL или 143 / STARTTLS</td><td>—</td></tr>
</table>

<h3 id="mailboxes">Раздел «Почтовые ящики»: пароли, даты копий, отборы</h3>
<img class="shot" src="img/03_accounts.png" alt="Почтовые ящики">
<div class="cap">Колонки «Вход» и «Резервные копии»: итог проверки пароля, даты последней и первой копии</div>
<p>В таблице ящиков видно главное: колонка <b>«Вход»</b> — итог последней проверки пароля («✓ пароль верный»,
«✗ неверный пароль», «нет связи», «не проверялся»), колонка <b>«Резервные копии»</b> — дата последней и первой
удачной копии, число писем и объём, а если последняя попытка сорвалась — её время красным. Ссылка
«история» открывает все прогоны копирования ящика.</p>
<ul>
<li><b>🔐 Проверить пароли</b> — фоновое задание входит в каждый ящик (IMAP LOGIN) и сразу выходит, письма не
скачиваются. Можно проверить все ящики, только включённые или только показанные в отборе. Не быстрее
4 ящиков одновременно: помните, что каждый неверный пароль — неудачный вход на почтовом сервере.</li>
<li><b>Отборы:</b> «С неправильным паролем», «Нет связи при проверке входа», «Пароль не проверялся»,
«Без резервных копий», «Копия старше 72 ч», «Последняя копия с ошибкой» и другие. Плашка «Требуют
внимания» на дашборде ведёт в эти же отборы.</li>
<li><b>🔑 Загрузить пароли</b> — таблица «адрес — пароль» (XLSX, CSV, XML). Строки, которые могли бы тихо
записать неверный пароль (колонки перепутаны, Excel превратил пароль в дату или TRUE), пропускаются с
причиной.</li>
<li>Меню ящика «⋯» → <b>«Завершить сеансы сотрудника»</b> — разлогинить сотрудника, вошедшего по паролю ящика.</li>
</ul>
<img class="shot" src="img/17_check_logins.png" alt="Проверка паролей">
<div class="cap">Проверка паролей: все ящики, только включённые или показанные в отборе</div>

<h3 id="mailadmin">Вход в ящики через администратора почты</h3>
<p>Многие почтовые серверы умеют пускать учётную запись администратора «от имени» ящика — тогда пароли
сотрудников не нужны вовсе, а смена пароля сотрудником копирование не ломает. Настройка — «Настройки →
🗝️ Вход в ящики через администратора почты».</p>
<table class="t">
<tr><th>Сервер</th><th>Поддержка</th></tr>
<tr><td>Dovecot</td><td>master users: «SASL PLAIN с authzid» или «Логин с разделителем» (<code>ящик*администратор</code>) — проверено вживую</td></tr>
<tr><td>Cyrus IMAP, Zimbra</td><td>«SASL PLAIN с authzid»</td></tr>
<tr><td>Axigen</td><td>такой вход по IMAP не документирован — проверьте кнопкой; если не работает, оставьте пароли ящиков</td></tr>
</table>
<ol class="steps">
<li>Заполните сервер, логин и пароль администратора и способ входа, включите и <b>сохраните</b>.</li>
<li>«Проверить вход администратора» — служба входит в любой ящик этого сервера и сразу выходит.</li>
<li>Если вход работает — «Перевести ящики сервера на вход через администратора». Пароли ящиков не удаляются:
«Вернуть вход по паролям» возвращает всё как было.</li>
</ol>
<div class="note"><b>Пароль администратора — только своему серверу</b>
Он отправляется только на указанный «Сервер администратора»; ящик с другим адресом сервера (опечатка или чужой
сервер) его не получит. В базе пароль хранится зашифрованным.</div>

<h3>Удержание архива и удаление вместе с письмами</h3>
<p>Меню ящика «⋯» → <b>«🔒 Удержание архива»</b>: до даты или бессрочно письма ящика не удаляются по сроку
хранения — для уволенных сотрудников, проверок и споров. <b>«🧹 Удалить ящик вместе с архивом писем»</b> удаляет и
файлы писем (подтверждается вводом названия ящика; недоступно, пока действует удержание). Обычное «Удалить
ящик» файлы писем оставляет.</p>

<h2 id="employees">9. Сотрудники</h2>
<p>Раздел <b>«Сотрудники»</b> (виден только администратору) — справочник людей организации и связь каждого
из них с почтовым ящиком. В разделе «Почтовые ящики» видны технические записи: адрес, сервер, логин; по ним
не всегда понятно, чья это почта. Справочник добавляет ФИО, должность и отдел и позволяет завести ящики
сразу на весь список из кадровой выгрузки, а не по одному вручную.</p>
<p>Что можно делать: добавить сотрудника, изменить карточку (✏️), удалить (✕), загрузить список из файла
(«⬆️ Импорт из файла»), синхронизировать с источником из настроек («↻ Синхронизировать»), посмотреть шаблон
создаваемых ящиков («🧩 Шаблон ящиков») и скачать готовый образец файла («📄 Образец файла»). Сверху —
счётчики: сколько сотрудников, у скольких есть ящик и у скольких его нет. Под заголовком — плашка источника:
откуда берётся список, включено ли расписание и кнопка «🔍 Проверить источник», которая читает источник и
показывает число строк, ничего не меняя в справочнике. Ниже — поиск по ФИО, почте и отделу.</p>
<div class="note"><b>Удаление сотрудника не трогает почту</b>
Удаляется только карточка из справочника. Почтовый ящик остаётся в разделе «Почтовые ящики», а локальные
копии писем — на диске.</div>

<h3>Файл со списком</h3>
<p>Подходят <b>CSV</b> и <b>Excel (.xlsx)</b>. Первая строка — заголовки, порядок столбцов любой, лишние
столбцы игнорируются. Названия распознаются без учёта регистра: ФИО (или «Имя», «Сотрудник»), E-mail
(«Почта», «Адрес»), Должность, Отдел, Телефон, Табельный номер («ID», «Код»). Кодировка определяется
автоматически — UTF-8 и Windows-1251.</p>
<pre><code>Табельный номер;ФИО;E-mail;Должность;Отдел;Телефон
1024;Иванов Иван Иванович;ivanov@example.ru;Менеджер;Продажи;+7 900 111-22-33</code></pre>
<p>После загрузки показывается отчёт: сколько карточек добавлено и обновлено, сколько ящиков создано и
привязано, и список строк, которые не удалось разобрать — с номером строки и причиной.</p>

<h3>Форматы выгрузки</h3>
<p>Читаются <b>CSV</b>, <b>Excel (.xlsx)</b> и <b>«Таблица XML 2003»</b> (SpreadsheetML) — в таком виде
выгружают списки интранет-справочники и корпоративные системы. Формат определяется <b>по содержимому</b>,
поэтому неважно, как называется файл и что отдаёт сервер по ссылке (<code>.xls</code>, <code>.xml</code>
или вовсе без расширения).</p>
<p>В XML-выгрузках разбираются их особенности: ФИО тремя колонками («фамилия», «имя», «отчество»)
склеивается в одно; колонка без заголовка со значениями «да»/«нет» читается как признак «работает»
(строки с «нет» в справочник не попадают и ящик им не заводится); пустой заголовок сразу после «имени» —
это отчество; из нескольких телефонов в карточку идёт мобильный, затем рабочий, затем внутренний;
HTML внутри ячейки убирается. Такие файлы часто невалидны как XML — разбор к этому устойчив.</p>
<p>Если один адрес встречается у нескольких сотрудников (общий ящик склада или сервиса), карточка
заводится одна, а в итоге синхронизации сообщается, какие адреса повторяются.</p>

<h3>Пароли ящиков из файла</h3>
<p>Ящики, заведённые сотрудникам автоматически, создаются без пароля. Чтобы не открывать сотни карточек
по одной: «Почтовые ящики» → <b>«🔑 Загрузить пароли»</b>. Файл — XLSX, CSV или XML-таблица из двух
колонок: <b>адрес ящика</b> и <b>пароль</b>; заголовки необязательны. Ящик ищется по логину, а если
такого логина нет — по адресу сотрудника из справочника. Галочка «Включить ящики после установки
пароля» сразу включает выключенные. В отчёте видно, сколько ящиков обновлено и включено, для каких
адресов ящик не нашёлся и какие строки не разобраны.</p>
<div class="warn"><b>⚠ Что происходит с паролями</b>
Пароли сохраняются в базе зашифрованными, в журнал и обратно в интерфейс не попадают, а загруженный
файл удаляется с сервера сразу после разбора — и при успехе, и при ошибке. В аудит пишутся только
счётчики и имя файла.</div>

<h3>Откуда берётся список: файл или URL</h3>
<p>Источник выбирается настройкой <b>«Источник списка»</b> в «Настройки» → «Сотрудники».</p>
<ul>
<li><b>Файл на сервере</b> — служба читает CSV/XLSX по пути на диске, например
<code>/var/lib/mailarchiver/hr/employees.csv</code>. Подходит, когда кадровая система кладёт файл на сервер
или в смонтированную сетевую папку. Файл читает служба <code>mailarchiver</code> — у неё должны быть права
на чтение.</li>
<li><b>Адрес (URL)</b> — служба сама скачивает выгрузку по ссылке <code>http(s)://</code> перед каждой
синхронизацией; копировать файл на сервер архива не нужно. Можно задать логин и пароль (HTTP Basic),
таймаут, проверку сертификата и формат ответа. Пароль хранится в базе зашифрованным и в интерфейс не
возвращается.</li>
</ul>
<p>Загрузка по ссылке проверяет ответ сама: HTML-страница входа вместо файла, ответы 401/403, размер больше
20 МБ и неверно угаданный формат объясняются понятным текстом, а не ошибкой разбора. XLSX опознаётся по
содержимому, даже если в ссылке нет расширения.</p>
<div class="tip"><b>💡 Проверяйте источник кнопкой</b>
«🔍 Проверить источник» читает файл или скачивает ссылку и показывает, сколько строк нашлось и какие из них
проблемные. Справочник при этом не меняется — можно спокойно проверять адрес до включения расписания.</div>

<h3>Автоматическая синхронизация</h3>
<p>Настраивается в «Настройки» → раздел «Сотрудники»: включение, источник (файл или URL), расписание
(по умолчанию <code>0 5 * * *</code> — ежедневно в 05:00) и признак «заводить ящики новым сотрудникам».
Синхронизация выполняется обычным фоновым заданием, поэтому её ход виден в разделе «Очередь и задания».</p>
<p>Правила сопоставления: сначала по табельному номеру, при его отсутствии — по адресу почты. Новые карточки
добавляются, у существующих обновляются только заполненные поля — <b>пустые значения в файле ничего не
затирают</b>.</p>
<div class="note"><b>Уволенные не обрабатываются автоматически</b>
Сотрудники, исчезнувшие из файла, остаются нетронутыми: ничего не выключается и не удаляется. Это защита
от того, чтобы сбой выгрузки или случайно урезанный файл не остановили копирование почты у половины
организации. Уволенных администратор обрабатывает вручную.</div>
<h3>Шаблон настроек создаваемых ящиков</h3>
<p>Когда сервис заводит ящик сотруднику — при синхронизации или при добавлении карточки вручную — он берёт
настройки из <b>шаблона</b> в «Настройки» → «Сотрудники». Шаблон задаёт сервер, порт и шифрование, название
и логин ящика, заметку, включать ли ящик сразу, фильтры папок, срок хранения и — при желании — расписание
копирования для нового ящика. Посмотреть результат, не заводя ящиков, можно кнопкой «🧩 Шаблон ящиков».</p>
<table>
<tr><th>Параметр шаблона</th><th>Что задаёт</th></tr>
<tr><td>Название ящика (шаблон)</td><td>Как ящик называется в списке, напр. <code>{full_name} ({department})</code></td></tr>
<tr><td>Логин ящика (шаблон)</td><td>Имя пользователя на сервере: <code>{email}</code> либо <code>{local}</code>, если входят коротким именем</td></tr>
<tr><td>Заметка (шаблон)</td><td>Текст в поле «Заметки», напр. <code>Создан автоматически: {position}, {department}</code></td></tr>
<tr><td>Включать созданные ящики</td><td>Включать сразу или оставлять выключенным до ввода пароля</td></tr>
<tr><td>Папки: копировать / пропускать</td><td>Фильтры папок нового ящика, напр. пропускать «Спам, Корзина»</td></tr>
<tr><td>Срок хранения</td><td><code>-1</code> — как в общих настройках, <code>0</code> — вечно, <code>N</code> — N дней</td></tr>
<tr><td>Расписание копирования</td><td>Создавать ли новому ящику расписание и с каким cron-выражением</td></tr>
</table>
<p>Подстановки в шаблонах названия, логина и заметки: <code>{full_name}</code>, <code>{last_name}</code>,
<code>{first_name}</code>, <code>{email}</code>, <code>{local}</code>, <code>{domain}</code>,
<code>{position}</code>, <code>{department}</code>, <code>{external_id}</code>. Пустые значения и оставшиеся
от них скобки убираются: <code>{full_name} ({department})</code> для сотрудника без отдела даст просто ФИО.</p>
<div class="tip"><b>💡 Пароль шаблон не задаёт никогда</b>
Сервис не знает паролей от почты сотрудников. Даже ящик, созданный «включённым», не начнёт копирование,
пока администратор не впишет пароль в разделе «Почтовые ящики». Чтобы новые ящики сразу попадали в ночное
копирование, включите в шаблоне «Заводить расписание копирования».</div>

<h3 id="dismissed">Уволенные сотрудники</h3>
<p>Ящик уволенного на почтовом сервере обычно удаляют, а переписка может понадобиться через годы. Когда
сотрудник становится уволенным — вручную (статус «Уволен (архив удерживается)» в карточке), по выгрузке
(«Работает: нет», «Уволен: да», прошедшая «Дата увольнения») или по отсутствию в выгрузке N дней, —
MailArchiver:</p>
<ol class="steps">
<li>сразу ставит <b>последнюю копию</b> ящика в очередь (пока ящик ещё есть на сервере);</li>
<li>после неё <b>выключает копирование</b> ящика;</li>
<li><b>удерживает архив</b> N лет (по умолчанию 5): письма не удаляются по сроку хранения, даже если у ящика
срок «3 дня».</li>
</ol>
<img class="shot" src="img/26_employees_dismissed.png" alt="Уволенный сотрудник">
<div class="cap">Уволенный сотрудник: копирование выключено, архив удерживается до указанной даты</div>
<p>Если одна синхронизация отмечает уволенными больше 20 % работающих, это похоже на испорченную выгрузку:
увольнения не применяются, а в итоге синхронизации появляется предупреждение. Вернувшийся сотрудник («Работает»)
получает ящик обратно — копирование включается, удержание из-за увольнения снимается. В разделе «Почтовые ящики»
есть отборы «Уволенные сотрудники», «Архив удерживается» и «Удержание истекло» (сам архив по истечении
удержания не удаляется — решение за администратором).</p>

<h2 id="backup">10. Резервное копирование</h2>
<p>Запустить копирование вручную можно из раздела <b>«Почтовые ящики»</b>:</p>
<ul>
<li><b>«💾 Копия сейчас»</b> в строке ящика (или пункт «Сделать резервную копию сейчас» в меню «⋯») —
копирование одного выбранного ящика;</li>
<li><b>«💾 Копия всех ящиков сейчас»</b> — кнопка вверху справа ставит в очередь копирование всех
включённых ящиков разом. Программа спросит подтверждение и покажет, сколько ящиков будет обработано.
Выключенные ящики пропускаются, а ящики, по которым копирование уже идёт, не запускаются повторно —
поэтому случайное повторное нажатие не удваивает нагрузку.</li>
</ul>
<p>Кроме того, копирование запускается автоматически по расписанию (раздел «Расписания»).</p>
<h3>Поиск и отбор ящиков</h3>
<p>Над списком — строка поиска (по названию, логину и серверу) и отбор по состоянию:
<b>все</b>, <b>только включённые</b>, <b>только выключенные</b>, <b>без пароля</b> и
<b>готовые к копированию</b> (включён и пароль задан). Рядом с каждым пунктом показано число ящиков,
а справа — «показано N из M». Это нужно, когда ящиков сотни: после синхронизации сотрудников они
заводятся выключенными и без пароля, и найти их глазами в общем списке невозможно.</p>
<p>Копирование инкрементальное — при повторных запусках докачиваются только новые письма, поэтому оно
быстрое. Прогресс виден в разделе «Очередь и задания».</p>

<h3>Скопировать заново</h3>
<p>Меню ящика «⋯» → <b>«🔄 Скопировать заново»</b>. Два режима:</p>
<ul>
<li><b>Докачать потерянные письма</b> (безопасный, выбран по умолчанию) — сервис сверяет индекс с
файлами на диске и, если файл письма пропал или оказался пустым (сбой диска, оборванный прогон,
чужая уборка), убирает запись из индекса, чтобы письмо скачалось заново. <b>Ничего не удаляется.</b></li>
<li><b>Полностью с нуля</b> — стирает локальную копию ящика (и файлы писем, и записи индекса) и
скачивает всё с сервера заново. Доступно только администратору, запрашивает подтверждение и
записывается в аудит.</li>
</ul>
<div class="warn"><b>⚠️ Режим «с нуля» необратим</b>
Письма, которых уже нет на почтовом сервере, существуют только в этой копии — после стирания их не
вернуть. Выбирайте этот режим, только если архив испорчен и нужен именно чистый лист; в остальных
случаях хватает «докачать потерянные».</div>
<div class="tip"><b>💡 Рекомендация</b>
Настройте расписание на ночное время (например, <code>0 3 * * *</code>). Так копирование не будет мешать
работе и меньше нагрузит почтовый сервер.</p></div>

<h2 id="export">11. Экспорт в PST (и EML/MBOX)</h2>
<p>Из локальной копии можно получить файл для Outlook. Откройте ящик (меню «⋯») → <b>«Экспорт»</b>.</p>
<img class="shot" src="img/10_export_modal.png" alt="Экспорт в PST">
<div class="cap">Окно экспорта: выбор движка, формата PST и фильтров</div>

<h3>Какой движок выбрать</h3>
<table class="t">
<tr><th>Движок</th><th>Формат</th><th>Надёжность</th><th>Когда использовать</th></tr>
<tr><td>Aspose PST <span class="badge ok">надёжно</span></td><td>.pst</td><td>Высокая</td><td>Нужен гарантированный .pst. Требует библиотеки Aspose.Email и лицензии для продакшена.</td></tr>
<tr><td>Встроенный PST <span class="badge exp">эксперим.</span></td><td>.pst</td><td>Средняя</td><td>Бесплатно, без зависимостей. Проверяйте результат в своём Outlook.</td></tr>
<tr><td>EML <span class="badge ok">надёжно</span></td><td>каталог .eml (.zip)</td><td>Высокая</td><td>Без потерь. Письма можно перетаскивать в Outlook, хранить как архив.</td></tr>
<tr><td>MBOX <span class="badge ok">надёжно</span></td><td>.mbox (.zip)</td><td>Высокая</td><td>Импорт в Thunderbird и конвертеры в Outlook.</td></tr>
</table>

<div class="note"><b>Почему .pst — особый случай</b>
Формат PST — закрытый формат Microsoft. На Linux <b>надёжно</b> создавать корректный .pst умеет по сути только
коммерческая библиотека Aspose.Email; полностью бесплатных генераторов .pst под Linux не существует
(open-source умеет только читать .pst). Поэтому MailArchiver предлагает: надёжный путь через Aspose, встроенный
бесплатный экспериментальный генератор (папки, тема, адреса, дата и текст — <b>без вложений</b>) и всегда доступные
EML/MBOX, которые Outlook импортирует без потерь.</p></div>

<h3>Версии Outlook: ANSI или Unicode</h3>
<p>Параметр «Формат PST» определяет совместимость:</p>
<ul>
<li><b>Unicode</b> — для Outlook 2003 и новее (2007/2010/2013/2016/2019/2021/365). Большой объём, поддержка любых языков. <b>Рекомендуется.</b></li>
<li><b>ANSI</b> — для очень старого Outlook 97–2002. Ограничение размера 2 ГБ.</li>
</ul>
<p>Просто выберите вашу версию Outlook в поле «Целевая версия Outlook» — формат подберётся автоматически.</p>
<p>Готовые файлы появляются в разделе <b>«Экспорт (PST)»</b>, откуда их можно скачать.</p>
<img class="shot" src="img/06_exports.png" alt="Список экспортов">
<div class="cap">Раздел «Экспорт»: готовые файлы для скачивания</div>

<h3>Как открыть .pst в Outlook</h3>
<ol><li>Скачайте файл .pst из раздела «Экспорт».</li>
<li>В Outlook: <b>Файл → Открыть и экспортировать → Открыть файл данных Outlook</b> и выберите скачанный .pst.</li>
<li>Папки появятся в левой панели Outlook.</li></ol>

<h2 id="restore">12. Восстановление на сервер</h2>
<p>Если письма пропали на почтовом сервере, их можно вернуть из локальной копии. Откройте ящик (меню «⋯»)
→ <b>«Восстановить на сервер»</b>.</p>
<div class="tip"><b>💡 Безопасный порядок</b>
1) Сначала сделайте <b>пробный прогон</b> (галочка «Пробный прогон») — он посчитает письма, но ничего не зальёт.
2) Заливайте в режиме <b>«В папки с префиксом»</b> (например, «Восстановлено») — так восстановленные письма не
смешаются с текущей почтой. 3) Оставьте включённой галочку «Пропускать дубли» — можно безопасно
перезапускать.</p></div>
<p>Восстановление сохраняет флаги (прочитано/важное) и дату получения писем.</p>

<h3>Импорт из существующего .pst</h3>
<p>Меню ящика → <b>«Импорт из .pst»</b>: загрузите PST-файл, и его письма зальются на IMAP-сервер в папку с
указанным префиксом. Для этого на сервере должен быть установлен <code>readpst</code> (пакет pst-utils) —
скрипт установки ставит его автоматически, в Docker он уже включён.</p>

<h2 id="retention">Хранение копий: 3 дня / 1 неделя</h2>
<p>Можно ограничить, за какой период хранить локальные копии писем каждого ящика — например, только за
последние <b>3 дня</b> или <b>1 неделю</b> от текущей даты. Копии старше выбранного срока удаляются
автоматически (ежедневная очистка). На письма на самом почтовом сервере это не влияет.</p>
<img class="shot" src="img/14_retention.png" alt="Хранение копий">
<div class="cap">Пресеты хранения: последние 3 дня, последняя неделя, хранить всё или свой срок</div>
<p>Открыть: меню ящика (⋯) → <b>«Хранение копий»</b> (в том числе свой срок в днях), либо задать в форме
ящика поле «Хранение локальных копий». Очистку раз в сутки запускает планировщик для всех ящиков, у
которых есть что удалять (время — «Настройки → Хранение и очистка → Время ежедневной очистки», по
умолчанию 04:30). Значение «по глобальной настройке» берёт срок «Хранить дней» из раздела «Хранение и
очистка» — он действует, только когда там включена <b>«Общая очистка по сроку»</b>.</p>
<p>Удалённые по сроку письма не скачиваются следующим копированием заново. Если срок <b>увеличить</b>
(или выбрать «хранить всё»), письма, которые ещё лежат на сервере, снова попадут в копию.</p>
<div class="warn"><b>⚠ Обратите внимание</b>
Ретеншн удаляет именно локальные копии старше срока. Если нужен полный «вечный» архив — оставьте
«Хранить всё». Хранение за 3 дня/неделю удобно, когда важны только свежие письма и экономия места.</div>

<h2 id="schedules">13. Расписания</h2>
<p>Раздел <b>«Расписания»</b> позволяет запускать бэкап (или очистку/проверку) автоматически.</p>
<img class="shot" src="img/07_schedules.png" alt="Расписания">
<div class="cap">Расписания автоматического копирования</div>
<p>Тип «по времени (cron)» задаётся выражением из пяти полей: <code>минуты часы день месяц день_недели</code>.</p>
<table class="t">
<tr><th>Выражение</th><th>Значение</th></tr>
<tr><td><code>0 3 * * *</code></td><td>каждый день в 03:00</td></tr>
<tr><td><code>0 */6 * * *</code></td><td>каждые 6 часов</td></tr>
<tr><td><code>30 2 * * 1</code></td><td>каждый понедельник в 02:30</td></tr>
<tr><td><code>0 22 * * 1-5</code></td><td>по будням в 22:00</td></tr>
</table>
<p>Часовой пояс расписаний задаётся в настройках (по умолчанию Europe/Moscow).</p>

<h2 id="settings">14. Настройки — справочник всех параметров</h2>
<p>Раздел <b>«Настройки»</b> сгруппирован по темам. У каждого параметра есть подсказка «?» с описанием,
рекомендацией и примером. Ниже — полный справочник тех же параметров.</p>
<img class="shot" src="img/09_settings_help.png" alt="Настройки с подсказкой">
<div class="cap">Подсказка к параметру: описание, рекомендация (💡), пример и значение по умолчанию</div>

%%PARAMS%%

<h2 id="jobs">15. Очередь и логи</h2>
<p>Раздел <b>«Очередь и задания»</b> показывает все задания, их статус и прогресс. Можно открыть детали
(журнал событий задания), отменить выполняющееся или повторить упавшее.</p>
<img class="shot" src="img/05_jobs.png" alt="Очередь заданий">
<div class="cap">Очередь и задания с прогрессом и действиями</div>
<p>Раздел <b>«Логи»</b> показывает системный журнал сервиса с фильтром по уровню важности.</p>
<img class="shot" src="img/11_logs.png" alt="Логи">
<div class="cap">Просмотр логов сервиса</div>

<h2 id="security">16. Безопасность</h2>
<ul>
<li><b>Вход по паролю.</b> Пароли пользователей хранятся в виде необратимого хеша (PBKDF2-HMAC-SHA256).
Защита от подбора блокирует конкретный источник, а не учётную запись: 5 неудач с одного адреса —
блокировка этого адреса для учётной записи, 50 по всем учётным записям — блокировка адреса; попытки со
всех адресов на одну учётную запись только замедляют ответ, чтобы злоумышленник не мог «выключить» вход
владельцу.</li>
<li><b>Шифрование секретов ящиков.</b> Пароли и токены IMAP-ящиков хранятся в БД в зашифрованном виде (Fernet/AES). Ключ лежит в файле <code>secret.key</code> с правами только для владельца.</li>
<li><b>Сессии.</b> Cookie сессии подписаны и ограничены по времени; есть авто-выход при бездействии —
открытая, но забытая вкладка сеанс не продлевает. Когда сеанс закончился, интерфейс показывает форму
входа и возвращает в тот же раздел.</li>
<li><b>Письма в браузере.</b> HTML письма показывается в песочнице без скриптов и с собственной политикой
безопасности: внешние картинки, стили и шрифты (трекинг-пиксели) не загружаются, пока вы не нажмёте
«Показать картинки».</li>
</ul>

<h3 id="twofa">Двухфакторный вход (2FA)</h3>
<img class="shot" src="img/18_2fa_setup.png" alt="Включение 2FA">
<div class="cap">Включение: QR-код для приложения и ключ для ручного ввода</div>
<p>После пароля запрашивается шестизначный код из приложения на телефоне (Яндекс Ключ, Google Authenticator,
Microsoft Authenticator, FreeOTP). Включение: своё имя внизу меню → отсканируйте QR-код → введите код →
<b>сохраните 10 резервных кодов</b> (каждый действует один раз, если телефона нет под рукой). Сделать 2FA
обязательной для всех администраторов: «Настройки → Безопасность → Обязательный двухфакторный вход».</p>
<ul>
<li>На один вход — 5 минут и 3 попытки ввода кода; смена пароля отзывает выданные «билеты» второго шага.</li>
<li>10 неверных кодов за 15 минут (или 50 за сутки) со всех адресов блокируют ввод кодов из приложения и
отправляют уведомление: пароль, видимо, известен постороннему — смените его. Резервные коды при этом
работают.</li>
<li>Потерян телефон: войдите резервным кодом; нет кодов — другой администратор сбросит 2FA в разделе
«Пользователи», единственный администратор — командой <code>sudo mailarchiver reset-2fa -u admin</code>.</li>
</ul>

<h3 id="encryption">Шифрование локальной копии писем</h3>
<p>По желанию письма на диске хранятся зашифрованными (AES-256-GCM, свой ключ на каждый файл). Просмотр,
экспорт и восстановление работают как обычно — расшифровка идёт «на лету». Включение: «Настройки →
Хранилище → Шифровать письма» (ключ будет создан в каталоге данных) или заранее созданный ключ вне
каталога данных:</p>
<pre class="cmd">sudo mailarchiver storage-key --generate /etc/mailarchiver/storage.key
sudo chown root:mailarchiver /etc/mailarchiver/storage.key && sudo chmod 640 /etc/mailarchiver/storage.key</pre>
<p>и в настройках: «Файл ключа» = этот путь, «Шифровать письма» = да. Уже сохранённые письма шифруются
кнопкой «Зашифровать уже сохранённые» в том же разделе.</p>
<div class="warn"><b>⚠ Храните копию ключа отдельно от сервера</b>
Без ключа зашифрованные письма не прочитать никак. Если ключ пропал, служба перестаёт сохранять новые
письма (чтобы они не легли на диск открытым текстом) и показывает на дашборде красную плашку —
верните файл ключа и перезапустите службу. Состояние ключа: <code>sudo mailarchiver storage-key</code>.</div>
<h3>Публикация через HTTPS (reverse proxy)</h3>
<p>Чтобы открыть доступ извне безопасно, поставьте перед сервисом nginx или traefik с TLS. Пример nginx:</p>
<pre class="cmd">server {
  listen 443 ssl;
  server_name mail-backup.example.ru;
  ssl_certificate     /etc/letsencrypt/live/.../fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/.../privkey.pem;
  location / {
    proxy_pass http://127.0.0.1:8493;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;    <span class="c"># для WebSocket</span>
    proxy_set_header Connection "upgrade";
  }
}</pre>
<p>В настройках включите «За обратным прокси», укажите адрес прокси в «Доверенные прокси», «Внешний URL»
(например <code>https://mail-backup.example.ru</code>) и включите «Cookie только по HTTPS».</p>

<h3 id="secjournal">Журнал безопасности</h3>
<p>Раздел <b>«🔐 Безопасность»</b>: действующие блокировки входа (пользователь с адреса, адрес целиком, коды 2FA,
проверки паролей ящиков) с кнопкой <b>«Снять блокировку»</b>, адреса с неудачами за сутки, последние попытки
входа и события безопасности — входы, 2FA, смена паролей, сеансы, поиск по почте.</p>
<img class="shot" src="img/21_security.png" alt="Журнал безопасности">
<div class="cap">Перебор паролей с одного адреса: видны блокировки, число неудач и кнопки снятия</div>

<h2 id="replica">17. Копия архива вне сервера и снимки базы</h2>
<p>Если сервер пропадёт целиком — сгорит диск, данные зашифрует вирус-вымогатель, по ошибке удалят виртуальную
машину, — вместе с ним пропадёт и архив. MailArchiver каждый день отправляет копию <b>в другое место</b>:</p>
<table class="t">
<tr><th>Куда</th><th>Как</th></tr>
<tr><td>Сетевая папка или диск</td><td>любой каталог, подключённый к серверу (SMB/CIFS, NFS, USB-диск)</td></tr>
<tr><td>Сервер по SSH</td><td>другой Linux-сервер, копирование rsync; вход по SSH-ключу службы</td></tr>
<tr><td>S3-хранилище</td><td>Yandex Object Storage, VK Cloud, Selectel, свой MinIO</td></tr>
</table>
<img class="shot" src="img/19_replica.png" alt="Копия вне сервера">
<div class="cap">«Настройки → Копия вне сервера и снимки базы»: параметры, итог последнего прогона и кнопки</div>
<ul>
<li>В копию уходят файлы писем (зашифрованные — как есть) и <b>снимки базы</b> — без базы копия писем была бы
грудой файлов. Снимки делаются каждый день (и кнопкой), проверяются на целостность, сжимаются и шифруются
ключом копии писем; 3 последних хранятся и на самом сервере.</li>
<li>Каждый день отправляется только новое; удалённое из архива удаляется и в копии — но не больше 50 % за
прогон без явного разрешения: так выглядит отключившийся диск, а не обычная очистка.</li>
<li>Метка <code>.mailarchiver-replica</code> не даёт писать копию в пустую точку монтирования отключённого диска
или в чужую папку: если метка пропала там, где копия уже была, прогон останавливается с понятной ошибкой.</li>
<li>Раз в неделю список файлов в копии сверяется с архивом целиком; если копия не обновлялась больше двух суток,
на дашборде появляется красная плашка.</li>
</ul>
<div class="warn"><b>⚠ Ключей в копии нет — и это правильно</b>
<code>secret.key</code> (им зашифрованы пароли ящиков) и ключ шифрования писем храните отдельно от копии: копия
вместе с ключами — не защищённая копия.</div>

<h3 id="restorecopy">Восстановление после аварии</h3>
<ol class="steps">
<li>Установите MailArchiver на новый сервер и остановите службу: <code>sudo systemctl stop mailarchiver</code>.</li>
<li>Скачайте копию: <code>sudo mailarchiver replica-pull --dir /mnt/backup/mailarchiver</code> (или
<code>--s3-endpoint … --s3-bucket … --s3-access-key … --s3-secret-key-file …</code>). Копию по SSH верните rsync-ом.</li>
<li>Положите <code>secret.key</code> в каталог данных и ключ шифрования — по пути из настроек.</li>
<li>Восстановите базу из свежего снимка: <code>sudo mailarchiver restore-snapshot mailarchiver-….db.gz[.enc]</code>.</li>
<li>Проверьте ключ (<code>sudo mailarchiver storage-key</code>) и запустите службу.</li>
</ol>
<p>Раз в квартал делайте пробное восстановление на тестовую машину: копия, из которой ни разу не
восстанавливались, может подвести в самый нужный момент.</p>

<h2 id="monitoring">18. Мониторинг: Prometheus, Zabbix, еженедельная сводка</h2>
<p><b>Еженедельная сводка</b> (по понедельникам в 8:00) приходит администраторам на почту: ящики, требующие
внимания (неверный пароль, давно не копировались, ошибки), итоги недели, место на диске и на сколько дней его
хватит, состояние копии вне сервера, неудачные входы. В карточке «Уведомления» — «Посмотреть сводку» и
«Отправить сейчас».</p>
<p><b>Метрики</b> <code>/metrics</code> в формате Prometheus включаются в «Настройки → Мониторинг»: ящики по
состояниям, давность последней копии каждого ящика, очередь, диск, копия вне сервера, снимки базы, неудачные
входы. Доступ — только с разрешённых адресов (по умолчанию с самого сервера) или по токену
<code>Authorization: Bearer …</code>.</p>
<img class="shot" src="img/25_monitoring.png" alt="Мониторинг">
<div class="cap">Метрики включены, токен задан; внизу — пример настройки Prometheus и Zabbix</div>
<pre class="cmd">scrape_configs:
  - job_name: mailarchiver
    scheme: https
    authorization: {type: Bearer, credentials: ТОКЕН}
    static_configs: [{targets: ["mail-archive.company.ru"]}]

<span class="c"># тревога: ящик включён, а свежей копии нет больше трёх суток</span>
- alert: MailboxBackupStale
  expr: (time() - mailarchiver_account_last_backup_timestamp_seconds) > 3*86400
        and mailarchiver_account_enabled == 1</pre>
<p>Zabbix: элемент «HTTP-агент» с URL метрик и заголовком <code>Authorization: Bearer ТОКЕН</code>, зависимые элементы
с предобработкой «Шаблон Prometheus», например <code>mailarchiver_accounts{state="bad_password"}</code>.</p>

<h2 id="trouble">19. Устранение неполадок</h2>
<table class="t">
<tr><th>Симптом</th><th>Причина и решение</th></tr>
<tr><td>«Аутентификация IMAP не удалась»</td><td>Неверный логин/пароль, либо провайдер требует «пароль приложения» или OAuth2. Включите IMAP в настройках почты, создайте пароль приложения.</td></tr>
<tr><td>«Ошибка TLS/SSL»</td><td>Неверный порт или режим шифрования. Проверьте: SSL — порт 993, STARTTLS — порт 143. Для внутренних серверов с самоподписанным сертификатом отключите «Проверять TLS-сертификат IMAP».</td></tr>
<tr><td>«Превышено время ожидания»</td><td>Медленная сеть/сервер. Увеличьте таймауты в настройках (backup.socket_timeout_s).</td></tr>
<tr><td>Экспорт .pst не открывается в Outlook</td><td>Встроенный движок экспериментальный. Используйте движок Aspose (с лицензией) либо экспортируйте в EML/MBOX.</td></tr>
<tr><td>Импорт .pst не работает</td><td>Не установлен <code>readpst</code>. Установите пакет pst-utils (Debian/Ubuntu) или libpst (RHEL).</td></tr>
<tr><td>Служба не запускается</td><td>Смотрите логи: <code>journalctl -u mailarchiver -n 50</code>. Проверьте конфигурацию: <code>mailarchiver check-config</code>.</td></tr>
<tr><td>Мало места на диске</td><td>Задайте срок хранения (у ящиков или общий с включённой «Общей очисткой по сроку»), включите сжатие писем (gzip), уменьшите «Хранить готовые выгрузки, дней», либо расширьте диск.</td></tr>
<tr><td>Забыт пароль администратора</td><td><code>sudo mailarchiver reset-password -u admin</code></td></tr>
<tr><td>Ящик в отборе «С неправильным паролем»</td><td>Сервер отверг логин или пароль. Исправьте пароль и нажмите «Проверить пароли». «Пароль не читается» — заменён файл secret.key: введите пароль заново.</td></tr>
<tr><td>Красная плашка «Копирование остановлено: нет ключа шифрования»</td><td>Шифрование включено, а файл ключа не найден или не подходит. Верните ключ и перезапустите службу.</td></tr>
<tr><td>«Слишком много неверных кодов … заблокирован»</td><td>Кто-то подбирает код 2FA: пароль, видимо, известен постороннему. Войдите резервным кодом и смените пароль.</td></tr>
<tr><td>«Время на ввод кода истекло или попытки исчерпаны»</td><td>На вход даётся 5 минут и 3 попытки кода. Введите пароль ещё раз; проверьте, что часы телефона выставлены автоматически.</td></tr>
<tr><td>При первичной настройке: «Запрос пришёл на незнакомое имя сервера»</td><td>Откройте интерфейс по IP-адресу или http://127.0.0.1:8493 (SSH-туннель), либо укажите имя во «Внешний URL».</td></tr>
<tr><td>«Сеанс завершён» и форма входа</td><td>Истёк срок сеанса или тайм-аут бездействия. Войдите снова — откроется тот же раздел.</td></tr>
<tr><td>«Ящики не заведены: в шаблоне ящиков не указан IMAP-сервер»</td><td>Укажите сервер в «Сотрудники → Шаблон ящиков» и запустите синхронизацию ещё раз.</td></tr>
<tr><td>«Пропуск папки … failed EXAMINE», «КОПИЯ НЕПОЛНАЯ»</td><td>Почтовый сервер отказался открыть папку — см. следующий раздел.</td></tr>
<tr><td>«Сервер вернул папку … повторно»</td><td>Сервер прислал папку в списке дважды. Вторая запись отбрасывается, копия не задваивается — делать ничего не нужно.</td></tr>
<tr><td>Копия вне сервера: «Папка для копии не найдена», «Метка копии … пропала»</td><td>Сетевая папка или диск не подключены (смонтированы). Папки в /home и /root службе не видны — подключайте в /mnt или /srv. Если место действительно новое — «Подготовить место».</td></tr>
<tr><td>Копия вне сервера: «Нет прав на запись»</td><td>Служба работает от пользователя mailarchiver: <code>sudo chown -R mailarchiver: ПУТЬ</code>, сетевую папку подключайте с <code>uid=mailarchiver,gid=mailarchiver</code>.</td></tr>
<tr><td>«Ключ сервера-получателя изменился»</td><td>Если сервер копии переустанавливали — удалите его строку из <code>replica_known_hosts</code> в каталоге данных. Иначе выясните, кто отвечает вместо него.</td></tr>
<tr><td>S3: «часы сервера расходятся с точным временем», «отказало в доступе»</td><td>Включите синхронизацию времени (<code>timedatectl set-ntp true</code>); проверьте ключи и права сервисного аккаунта на бакет.</td></tr>
<tr><td>«Удаление в копии приостановлено»</td><td>Прогон собирался удалить слишком много файлов — так выглядит отключившийся диск. Если удаление ожидаемо — «Разрешить удаление и запустить».</td></tr>
<tr><td>Поиск не находит письмо</td><td>Индекс ещё строится («Проиндексировано N из M» над строкой поиска) или при шифровании копии текст не индексируется — ищите по теме, адресам, вложениям.</td></tr>
<tr><td><code>/metrics</code>: 404, 401 или 403</td><td>404 — метрики выключены; 401/403 — нет верного токена и адрес не в списке «Адреса без токена».</td></tr>
<tr><td>«Вход через учётную запись администратора почты отклонён сервером»</td><td>Неверные логин/пароль администратора, не тот способ входа или сервер такой вход не умеет (Axigen).</td></tr>
<tr><td>«Выгрузка отмечает уволенными N сотрудников … НЕ применены»</td><td>Больше 20 % увольнений разом — похоже на испорченную выгрузку. Проверьте файл; настоящие увольнения отметьте вручную.</td></tr>
</table>

<h3>Папка не открывается: <code>failed EXAMINE</code></h3>
<p>Это ответ <b>почтового сервера</b>: он отказался открыть папку. Прежде чем объявить копию неполной,
MailArchiver делает три вещи:</p>
<ol>
<li>повторяет попытку через полторы секунды — часть отказов временная (ящик занят другой сессией);</li>
<li>пробует открыть папку обычным <code>SELECT</code> вместо <code>EXAMINE</code>: некоторые серверы
(замечено на Axigen) отказывают только на <code>EXAMINE</code>. Письма всё равно скачиваются командой
<code>BODY.PEEK</code> — отметки «прочитано» на сервере не появляются;</li>
<li>проверяет, есть ли у папки вложенные папки: если есть — это папка-контейнер, своих писем она не
хранит (сервер просто не пометил её флагом <code>\Noselect</code>), и копия остаётся полной;</li>
<li>спрашивает командой <code>STATUS</code>, сколько писем сервер видит в этой папке. <b>0 писем</b> —
копия остаётся полной, в журнале будет «папка ПУСТАЯ … потерь нет». <b>N писем</b> — задание помечается
«частично выполнено» и в итоге пишется «КОПИЯ НЕПОЛНАЯ» с перечнем папок.</li>
</ol>
<p>Что смотреть на сервере: открывается ли папка в веб-почте; имя папки (соседние имена вида
<code>s2022</code> и <code>s2022_000</code> — признак дубликата, который сервер создал сам при конфликте
имён, такие папки обычно и не открываются); права (ACL) учётной записи; не заблокирован ли ящик другой
сессией. В Axigen учётную запись чинят через CLI: <code>telnet 127.0.0.1 7000</code> →
<code>update domain name &lt;домен&gt;</code> → <code>REPAIR Accounts &lt;учётка&gt;</code>; восстановленная
копия создаётся под именем вида <code>&lt;учётка&gt;.00000015.repaired</code> и её нужно переименовать —
это крайняя мера, делайте её в окно обслуживания.</p>
<p>Всё, что сервис пробовал и узнал, попадает в текст ошибки: «Что пробовали: EXAMINE — отказ
(попыток: 2); обычный SELECT — отказ» и «Дополнительно: …». Строка «Дополнительно» отвечает на главный
вопрос — кто виноват: «в списке папок сервера это имя есть … сама папка нерабочая» (чинить на почтовом
сервере) либо «LIST по точному имени эту папку НЕ находит» (сервер не признаёт имя своим — беда с
кодировкой или невидимым символом, его сервис назовёт: например «невидимый символ NBSP (U+00A0)»).</p>
<h3>Папка не открывается давно — это перестаёт быть ошибкой</h3>
<p>Папка, которую сервер не отдаёт несколько прогонов подряд, переходит в разряд «известных
нечитаемых»: сервис продолжает пробовать её и продолжает писать о ней в итоге задания, но задание
больше не помечается неполным. Порог задаёт настройка <b>«Терпеть нечитаемую папку, прогонов»</b>
(Настройки → «Резервное копирование», по умолчанию 3; <code>0</code> — считать ошибкой всегда).
Сломанную на сервере папку из архива не починить, а вечное «КОПИЯ НЕПОЛНАЯ» приучает не читать
предупреждения — и следующая настоящая пропажа писем останется незамеченной. Как только сервер
отдаст папку, счётчик сбрасывается и письма копируются как обычно.</p>

<h3>Кнопка «Проверить папки на сервере»</h3>
<p>«Почтовые ящики» → «⋯» у ящика → <b>«🗂️ Проверить папки на сервере»</b>. Сервис опрашивает каждую
папку и показывает таблицу с вердиктом: <b>копируется</b> (письма доступны), <b>контейнер</b> (своих
писем не хранит, содержимое во вложенных папках), <b>пустая, не открывается</b> (терять нечего),
<b>НЕ ЧИТАЕТСЯ</b> (вот это и есть потеря — вверху видно, сколько писем недоступно) и
<b>исключена</b> (не копируется по настройкам ящика). Для нечитаемых там же есть кнопка
«🚫 Больше не копировать эти папки».</p>

<div class="tip"><b>💡 Если папку не починить</b>
Откройте «Очередь и задания» → «Детали» задания → блок «Копия неполная» → кнопка
<b>«🚫 Больше не копировать эти папки»</b>: папки добавятся в «Пропускать папки» этого ящика (убрать
можно в карточке ящика). Задание перестанет помечаться неполным из-за папок, которые на сервере всё
равно не восстановить, — и следующая настоящая пропажа писем будет заметна.</div>

<h2 id="faq">20. Частые вопросы</h2>
<h3>Это удалит письма с почтового сервера?</h3>
<p>Нет. Копирование только читает письма. Локальная копия — это надёжный архив, из которого ничего не
пропадает, даже если письмо удалят на сервере. Локальные письма удаляются только по сроку хранения,
который вы задали сами.</p>
<h3>Сколько ящиков можно подключить?</h3>
<p>Ограничений нет. Сколько заданий идёт одновременно, задаёт «Одновременных заданий» (1–16); с одним ящиком
одновременно работает только одно задание.</p>
<h3>Нужен ли интернет для экспорта в PST?</h3>
<p>Нет, экспорт локальный. Интернет нужен только движку Aspose при первичной установке библиотеки и для OAuth2.</p>
<h3>Где лежат копии писем?</h3>
<p>В каталоге данных (по умолчанию <code>/var/lib/mailarchiver/mailboxes</code>, в Docker — том <code>/data</code>).
Это обычные файлы .eml в структуре папок Maildir.</p>
<h3>Можно ли перенести копии на другой сервер?</h3>
<p>Да. Скопируйте каталог данных целиком (письма, база индексов, <code>secret.key</code>), <code>config.yaml</code> и —
если включено шифрование и ключ лежит вне каталога данных — <b>файл ключа шифрования</b>. Без него
зашифрованные письма не прочитать. Проверка на новом сервере: <code>sudo mailarchiver storage-key</code>.</p>
<h3>А если сервер сгорит целиком?</h3>
<p>Включите копию архива вне сервера (раздел 17): каждый день в сетевую папку, на другой сервер по SSH или в S3,
вместе со снимками базы. Восстановление — командами <code>replica-pull</code> и <code>restore-snapshot</code>.</p>
<h3>Можно ли не собирать пароли сотрудников?</h3>
<p>Если почтовый сервер пускает администратора в чужие ящики (Dovecot, Cyrus, Zimbra) — да, см. «Вход в ящики через
администратора почты». Axigen такой вход не документирует — проверьте кнопкой; если нет, остаются пароли ящиков.</p>
<h3>Что будет с почтой уволенного сотрудника?</h3>
<p>Сделается последняя копия, копирование выключится, архив будет удерживаться 5 лет (или сколько настроено) —
очистка по сроку его не тронет.</p>
<h3>Какие есть команды в консоли сервера?</h3>
<p><code>mailarchiver check-config</code>, <code>create-admin</code>, <code>reset-password -u ИМЯ</code>,
<code>reset-2fa -u ИМЯ</code>, <code>storage-key</code> (и <code>--generate ПУТЬ</code>), <code>replica-pull</code>,
<code>restore-snapshot СНИМОК</code>, <code>replica-ssh-key</code>, <code>version</code>.
Установщик создаёт команду <code>mailarchiver</code>; запущенная через <code>sudo</code>, она сама работает от
имени службы.</p>
<h3>Безопасно ли открывать доступ из интернета?</h3>
<p>Только через HTTPS (reverse proxy) и с включённой аутентификацией. Прямой доступ по HTTP наружу не рекомендуется.</p>

<h2 id="api">21. API</h2>
<p>Весь интерфейс работает поверх REST API. Его можно использовать для автоматизации. Полное описание —
по адресу <code>/api/docs</code> (интерактивная документация OpenAPI). Основные точки:</p>
<table class="t">
<tr><th>Метод</th><th>Путь</th><th>Назначение</th></tr>
<tr><td>POST</td><td>/api/login</td><td>вход (устанавливает cookie сессии)</td></tr>
<tr><td>GET</td><td>/api/state</td><td>сводка для дашборда</td></tr>
<tr><td>GET/POST</td><td>/api/accounts</td><td>список / создание ящиков</td></tr>
<tr><td>POST</td><td>/api/accounts/{id}/backup</td><td>запустить копирование</td></tr>
<tr><td>POST</td><td>/api/accounts/{id}/export</td><td>запустить экспорт</td></tr>
<tr><td>POST</td><td>/api/accounts/{id}/restore</td><td>запустить восстановление</td></tr>
<tr><td>GET</td><td>/api/jobs</td><td>список заданий и их прогресс</td></tr>
<tr><td>GET</td><td>/api/exports/{id}/download</td><td>скачать готовый файл экспорта</td></tr>
<tr><td>GET/PUT</td><td>/api/settings</td><td>чтение / изменение настроек</td></tr>
<tr><td>GET</td><td>/api/search?q=…&amp;scope=all</td><td>поиск по письмам (каждый поиск — в аудите)</td></tr>
<tr><td>GET, POST</td><td>/api/replica, /api/replica/run</td><td>состояние копии вне сервера, запуск прогона</td></tr>
<tr><td>POST</td><td>/api/replica/snapshot</td><td>снять снимок базы</td></tr>
<tr><td>GET</td><td>/api/analytics/dedup</td><td>отчёт «Одинаковые вложения»</td></tr>
<tr><td>GET, POST</td><td>/api/security, /api/security/unblock</td><td>журнал безопасности, снятие блокировки</td></tr>
<tr><td>POST</td><td>/api/accounts/{id}/hold</td><td>удержание архива ящика</td></tr>
<tr><td>GET</td><td>/metrics</td><td>метрики Prometheus (если включены; с разрешённых адресов или по токену)</td></tr>
</table>

<hr style="margin:40px 0;border:none;border-top:1px solid var(--bd)">
<p class="cap">MailArchiver · документация сгенерирована из исходного кода, поэтому справочник параметров
всегда соответствует установленной версии.</p>

</main>
</div>
</body>
</html>"""
